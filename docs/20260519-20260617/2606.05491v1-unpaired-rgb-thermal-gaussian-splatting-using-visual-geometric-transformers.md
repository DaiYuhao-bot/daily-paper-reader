---
title: Unpaired RGB-Thermal Gaussian-Splatting Using Visual Geometric Transformers
title_zh: 使用视觉几何变换器的非配对RGB-热高斯溅射
authors: "Jean Cordonnier, Chenghao Xu, Olga Fink, Malcolm Mielle"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05491v1"
tags: ["query:world-model"]
score: 9.0
evidence: 使用3D高斯喷溅的多模态新视角合成
tldr: 现有RGB-热成像多模态新视图合成通常依赖精确配准或立体设置，限制了可扩展性和实际部署。本文提出一种无配对框架，利用VGGT独立估计各模态相机位姿，通过Procrustes算法对齐，结合多模态3D高斯泼溅直接学习。实验表明该方法在热视图合成上性能优异，同时保持RGB保真度。此外，构建了跨模态一致性评估基准，推动了无校准多模态重建的发展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05491v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1605, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1799, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 1131, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 901, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05491v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1351, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05491v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 562, \"height\": 230, \"label\": \"Table\"}]"
motivation: 现有方法依赖精确校准的RGB-热图像对或立体设置，难以扩展，缺乏实用性。
method: 使用VGGT独立估计位姿，经Procrustes对齐跨模态特征，再以多模态3D高斯泼溅联合学习。
result: 在多样场景中热视图合成达到竞争性能，RGB保真度保持良好。
conclusion: 提出无配对RGB-热成像NVS框架，并引入跨模态一致性基准评估。
---

## 摘要
结合RGB和热成像的多模态新视角合成（NVS）能够利用视觉和热信息实现精确的三维场景重建。然而，现有方法通常依赖精确校准的RGB-热图像对或立体装置，限制了可扩展性和实际部署。为了解决这一问题，我们引入了一个非配对RGB-热NVS框架，该框架利用VGGT（一种三维前馈变换器架构）独立估计每个模态的相机姿态。然后通过普罗克鲁斯特算法结合跨模态特征匹配器对齐姿态集，实现无需配对校准的联合配准。在此基础上，我们进一步提出了一种多模态三维高斯溅射方法，直接从非配对的RGB和热图像中学习。在多种场景上的实验表明，我们的方法在热视图合成中取得了具有竞争力的性能，同时保持了RGB保真度。此外，我们指出现有的重建方法可能产生缺乏跨模态一致性的模态特定重建。因此，我们引入了一个基准框架来严格评估单模态图像合成以及重建场景的多模态一致性。

## Abstract
Multi-modal novel view synthesis (NVS) combining RGB and thermal imagery enables precise 3D scene reconstruction with visual and thermal information. However, existing methods typically rely on precisely calibrated RGB-thermal image pairs or stereo setups, limiting scalability and practical deployment. To address this, we introduce a framework for unpaired RGB-thermal NVS that leverages VGGT, a 3D feed-forward transformer architecture, to independently estimate camera poses for each modality. The pose sets are then aligned using the Procrustes algorithm with a cross-modal feature matcher, enabling joint registration without paired calibration. Building on this alignment, we further propose a multi-modal 3D Gaussian Splatting approach that learns directly from unpaired RGB and thermal images. Experiments on diverse scenes demonstrate that our method achieves competitive performance in thermal view synthesis while maintaining RGB fidelity. Moreover, we show that existing reconstruction approaches can produce modality-specific reconstructions that lack cross-modal consistency. We thus introduce a benchmarking framework to rigorously evaluate both per-modality image synthesis and the multi-modal coherence of reconstructed scenes.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有RGB-热成像多模态新视角合成（NVS）方法通常依赖精确校准的配对图像或立体设置（如ThermoNeRF、Thermal Gaussian Splatting），需要预处理中通过SfM估计相机位姿。热成像图像由于低对比度、纹理缺失和“鬼影效应”，SfM难以收敛，导致仅能在高质量热数据集上工作。立体设置成本高、校准复杂，限制了可扩展性和实际部署（如多机器人协同建图）。
- **目标**：提出非配对（unpaired）RGB-热NVS框架，允许RGB和热图像独立采集（不同视角、不同时间），无需配对校准，实现跨模态一致性重建。

## 2. 方法论
### 核心思想
- **解耦位姿估计与跨模态对齐**：分别使用VGGT（3D前馈Transformer架构）独立估计RGB和热图像的相机位姿；然后通过跨模态特征匹配（XoFTR）建立对应点，利用Procrustes算法估计相似变换（旋转、平移、尺度），将热相机位姿对齐到RGB坐标系；最后使用多模态3D高斯溅射（3DGS）联合学习场景表示。

### 关键技术细节
1. **独立位姿估计**：将单通道热图像复制为三通道，直接输入VGGT（无需微调），输出相机内外参、点图、深度图等。RGB和热图像各自独立运行VGGT，得到两组不对齐的位姿。
2. **跨模态特征匹配与对齐**：
   - 使用XoFTR建立RGB-热图像对间的2D对应点，经MAGSAC滤除异常值，仅保留对应点数量>阈值（12对）的图像对。
   - 利用VGGT输出的点云和置信度，将2D对应点投影到3D，得到两组3D对应点集 \(P_{\text{RGB}}\) 和 \(P_{\text{T}}\)，并丢弃置信度低于50%分位数的点。
   - 采用**修剪的Procrustes（Sim(3)）** 迭代优化：最小化 \( \sum \|p_{\text{RGB}} - (s R p_{\text{T}} + t)\|^2 \)，每次迭代后丢弃10%残差最大的点，重新计算变换。最终得到相似变换矩阵 \(T_{\text{Sim}(3)}\)，将热相机位姿转换到RGB坐标系（公式6）。
3. **多模态3DGS训练**：
   - 每个高斯体素包含位置 \(\mu\)、协方差 \(\Sigma\)、透明度 \(\alpha\)、RGB颜色 \(c\) 和热颜色 \(t\)（均含球谐编码，支持视角相关）。
   - **训练策略**：每个epoch随机打乱RGB和热图像，渲染一对随机RGB和热图像（无空间对齐约束），计算SSIM损失（式7）：\(L_{\text{total}} = L_{\text{SSIM}}(\text{Temp pred}, \text{Temp gt}) + L_{\text{SSIM}}(\text{RGB pred}, \text{RGB gt})\)，反向传播时视为配对数据。

## 3. 实验设计
- **数据集**：9个场景，源于ThermalGaussian和ThermoScenes数据集。排除XoFTR无法建立可靠对应的场景，保证公平评估核心贡献。
- **非配对数据集构造**：从每个原始图像对中随机抽取RGB或热图像，确保两种模态图像数量相等，完全消除跨模态对应关系。
- **基准方法**：**ThermalGaussian (T-GS)**，两种变体：
  - T-GS RGB：使用RGB位姿训练（理想配对情况）。
  - T-GS T：使用热位姿训练。
- **我们的方法变体**：
  - Ours RGB：训练使用本文对齐后的位姿，测试时用RGB位姿渲染。
  - Ours T：测试时用热位姿渲染。
- **评估指标**：
  - **图像质量**：PSNR、SSIM（RGB额外用LPIPS），热图像另用MAE（温度误差）。
  - **位姿质量**：相对位姿误差（RPE）和绝对轨迹误差（ATE），所有场景缩放到1m球内计算。
  - **Inter-modal vs Intra-modal指标**：
    - Intra-modal：RGB用RGB位姿，热用热位姿。
    - Inter-modal：RGB用热位姿，热用RGB位姿（检验跨模态一致性）。
- **对比分析**：定量结果在表I（9场景平均），定性结果在表II和图3。

## 4. 资源与算力
- **未明确说明**：文中未提及使用的GPU型号、数量、训练时长等算力信息。仅在实现参数中提到“Our GD implementation uses the same default parameters as nerfacto”，但未提供具体硬件配置。

## 5. 实验数量与充分性
- **场景数量**：9个场景，涵盖室内外、不同热纹理条件。
- **对比实验**：主要与T-GS（两种变体）对比，未与其他多模态方法（如ThermoNeRF）比较。
- **消融实验**：仅通过“手动尺度不变对齐”（图3）展示了不对齐时模型会学习两个独立表示，验证了对齐必要性。但未系统消融关键组件（如移除XoFTR、不同阈值、是否用VGGT等）。
- **公平性**：作者承认与T-GS比较不算完全公平（T-GS基于配对理想情况，本文基于非配对），但提供了上下界参考。Inter/Intra指标设计合理，能揭示跨模态一致性问题。
- **评估充分性**：实验覆盖了主要指标，但缺少对热图像特殊噪声（鬼影效应）的专门分析，且仅使用了XoFTR能处理的场景，存在选择偏差。

## 6. 主要结论与发现
- **性能**：非配对方法在热视图合成上达到竞争性能（甚至Ours T在热位姿下PSNR 25.595 vs T-GS T 25.107），同时保持RGB保真度。
- **对齐必要性**：Inter-modal指标明显低于Intra-modal（热PSNR从25.595降至21.315），说明位姿对齐不完美会导致跨模态不一致；若完全不对齐，模型会学到两个独立表示（图3）。
- **评估建议**：提出应同时使用inter-和intra-modal指标，避免仅依赖单模态指标产生虚假的高性能。
- **VGGT泛化性**：直接处理热图像（无需微调）仍能重建场景，体现3D前馈Transformer的泛化能力。

## 7. 优点
- **创新性**：首次实现无需配对的RGB-热NVS，解决实际部署中数据采集异构的难题。
- **方法简洁**：利用VGGT的强泛化性替代SfM，避免繁琐的校准和热图像特征提取困难。
- **对齐策略**：结合XoFTR（跨模态匹配）和修剪Procrustes，鲁棒性较好（通过迭代丢弃异常点）。
- **训练技巧**：随机打乱配对训练，简单有效处理非配对数据。
- **评估体系**：提出inter/intra-modal指标，揭示跨模态一致性问题，对后续工作有重要指导意义。

## 8. 不足与局限
- **实验局限性**：
  - 仅选择XoFTR能成功匹配的场景，排除了低纹理、极端热变化场景，泛化性受限。
  - 未与ThermoNeRF等非3DGS方法对比。
  - 缺乏系统性消融实验（如不同匹配阈值、点云置信度阈值的影响）。
- **对齐精度**：Inter-modal指标下降明显（热PSNR下降约4dB），表明Procrustes对齐仍有误差，可能源于VGGT位姿估计偏差或跨模态匹配噪声。
- **资源未报告**：无法评估计算开销和可复现性。
- **应用限制**：需要测试集具有配对图像（用于inter-modal评估），实际部署中若无可信外部位姿则无法验证。
- **未处理热成像特有噪声**：如“鬼影效应”对重建质量的影响未专门讨论。

（完）
