---
title: "E3DGS: Unified Geometric-Photometric Equivariance for 3D Gaussian Splatting via Color-as-Geometry Embedding"
title_zh: E3DGS：通过颜色即几何嵌入实现三维高斯泼溅的统一几何-光度等变性
authors: "Chankyo Kim, Maani Ghaffari"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15536v1"
tags: ["query:world-model"]
score: 8.0
evidence: 等变三维高斯泼溅用于场景生成
tldr: 3D高斯泼溅中颜色被视为信号而非几何实体，导致SE(3)等变构建困难。通过表示论证明，低阶球谐系数与秩2几何张量同构，可映射为3x3矩阵的共轭作用。提出统一矩阵嵌入，将所有属性提升至gl(3)空间，构建刚体等变架构E3DGS，避免克莱布施-戈登张量积。在物体视觉和动作条件高斯世界建模中，该方法在相机变换下鲁棒性更强，数据效率更高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15536v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1628, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1701, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1535, \"height\": 777, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 611, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1671, \"height\": 1179, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 720, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15536v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1624, \"height\": 896, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15536v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15536v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 903, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15536v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1704, \"height\": 419, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15536v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1484, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15536v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1715, \"height\": 335, \"label\": \"Table\"}]"
motivation: 现有3DGS等变方法因颜色与几何异质处理而破坏对称性，需统一几何-光度等变。
method: 基于表示论，将低阶球谐系数映射为几何张量，提出统一矩阵嵌入至gl(3)空间，构建SE(3)等变架构。
result: 在物体视觉和动作条件建模任务中，相机变换下鲁棒性显著提升，数据效率更优。
conclusion: E3DGS通过颜色即几何的嵌入，实现了几何与光度统一的等变处理，无需复杂张量积。
---

## 摘要
三维高斯泼溅（3DGS）通过将显式几何（位置、协方差）与视角依赖的光度（球谐函数）耦合来捕捉场景。然而，在这些基元上构建SE(3)等变架构存在根本性的表示瓶颈。颜色一直被视为信号而非几何实体，导致在相机坐标系变化时难以统一几何与外观的对称性。虽然平移通过相对坐标处理，但旋转对属性的作用是非均匀的：μ ↦ Rμ, Σ ↦ RΣR^⊤, f_ℓ ↦ D^ℓ(R)f_ℓ。这种不匹配使严格的等变性复杂化，导致现有方法要么丢弃球谐系数，要么将其扁平化，从而破坏对称性。我们从表示论出发提出统一解决方案：对于球谐阶数ℓ ≤ 2，光度与秩2几何张量代数同构。我们证明了这些球谐系数上的Wigner-D作用可以精确重新表述为3×3矩阵上的共轭作用。利用这一点，我们引入统一矩阵嵌入，一种将所有高斯属性提升到统一载波空间gl(3)的映射。基于“颜色即几何”公式，我们提出了E3DGS，一种无需Clebsch-Gordan张量积即可处理三维高斯的刚体（SE(3)）等变架构。在物体视觉和动作条件高斯世界建模上的评估表明，我们的统一方法在相机坐标系变化下具有强鲁棒性，并提升了数据效率。

## Abstract
3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position, covariance) with view-dependent photometry (Spherical Harmonics). However, building $\mathrm{SE}(3)$-equivariant architectures on these primitives presents a fundamental representation bottleneck. Color has been treated as a signal rather than a geometric entity, making it nontrivial to unify symmetry across geometry and appearance as the camera frame changes. While translations are handled by relative coordinates, rotations act heterogeneously across attributes: $μ\mapsto Rμ$, $Σ\mapsto RΣR^\top$, and $f_\ell\mapsto D^\ell(R)f_\ell$. This mismatch complicates strict equivariance, leading existing methods to either discard or flatten SH coefficients, thereby breaking symmetry. We propose a unified solution rooted in representation theory: for SH degrees $\ell\le2$, photometry is algebraically isomorphic to a rank-2 geometric tensor. We prove that the Wigner-$D$ action on these SH coefficients can be exactly reformulated as the conjugation action on $3\times3$ matrices. Leveraging this, we introduce the Unified Matrix Embedding, a lifting that maps all Gaussian attributes into a unified carrier space, $\mathfrak{gl}(3)$. Building on the "Color-as-Geometry" formulation, we present E3DGS, a rigid-body ($\mathrm{SE}(3)$) equivariant architecture that processes 3D Gaussians without Clebsch-Gordan tensor products. Evaluations on object vision and action-conditioned Gaussian world modeling demonstrate that our unified approach yields strong robustness under camera-frame changes and improved data efficiency.

---

## 论文详细总结（自动生成）

## 论文总结：E3DGS: Unified Geometric–Photometric Equivariance for 3D Gaussian Splatting via Color-as-Geometry Embedding

### 1. 核心问题与整体含义
- **研究动机**：3D高斯泼溅（3DGS）将显式几何（位置、协方差）与视角依赖的光度（球谐函数SH）耦合，但颜色被当作**信号而非几何实体**，导致在相机坐标系变化（SE(3)变换）下，几何与光度的**变换规则不统一**（几何用旋转矩阵，SH用Wigner-D矩阵），使得构建严格SE(3)等变的深度学习架构面临**表示瓶颈**。
- **问题影响**：现有方法为规避旋转等变的复杂性，通常丢弃高阶SH系数（仅保留球谐零阶的漫反射颜色），从而丢失了视角依赖的外观信息（如光泽、纹理），限制了模型区分几何相同但材质不同的物体的能力。
- **核心目标**：提出一种统一的表示，使几何与光度在SE(3)变换下遵循**同一个变换规则**（矩阵共轭），从而实现**严格的等变性**，提升鲁棒性和数据效率。

### 2. 方法论
- **核心思想**：基于表示论，证明对于球谐阶数ℓ ≤ 2，光度与**秩2几何张量代数同构**，可将SH系数映射到3×3矩阵空间gl(3)；进而将所有高斯属性（位置、协方差、SH系数）提升（lifting）到同一个gl(3)载波空间中，使它们在SE(3)旋转下统一按照**矩阵共轭**规则（M → R M R^⊤）变换。
- **关键技术细节**：
  - **统一矩阵嵌入（Unified Matrix Embedding）**：
    - **位置**：通过**hat map**将中心化坐标¯μ映射为so(3)矩阵：P_i = [¯μ_i]×。
    - **协方差**：通过**矩阵对数**映射为对称矩阵：C_i = log(Σ_i) ∈ Sym(3)。
    - **光度**：为每个颜色通道c，构造线性等变映射Φ≤2，将SH系数（ℓ=0,1,2）嵌入gl(3)：（ℓ=0 → 标量矩阵；ℓ=1 → so(3)矩阵；ℓ=2 → 对称无迹矩阵）。
    - 最终每个高斯体素得到一组活跃矩阵通道H_i ∈ gl(3)^{C_eq}，以及一组不变标量特征s_i（透明度、SH0、任务语义等）。
  - **E3DGS架构**：
    - 基于**Ad-等变**的Reductive Lie Neuron (ReLN) 构建块，包括ReLN-Attention（用Killing形式计算注意力权重，保证不变性）、ReLN-LayerNorm（对Frobenius范数做LayerNorm）、ReLN-Linear/ReLU（不变门控非线性）。
    - 整个编码器对活跃通道保持严格的SO(3)等变性：F_θ(Ad_R(H), s) = (Ad_R(H'), s')。
    - **无需Clebsch–Gordan张量积**，计算开销低（每对相互作用仅O(1)常数成本）。
  - **任务头**：
    - **不变读出头**：通过Killing形式自收缩提取不变标量，用于分类等任务。
    - **等变预测与逆提升**：对于动力学预测，输出矩阵经skew投影和vee map得到向量，保证SE(3)等变。

### 3. 实验设计
- **数据集与基准**：
  - **物体识别**：使用**ShapeSplat**数据集（基于ShapeNet的三维高斯资产）进行自监督预训练，然后在**ModelNet10-4K**和**ModelNet40**上评估分类；部分分割在**ShapeNet Part**上评估。
  - **动作条件高斯世界建模**：在**RLBench**多任务设置（10个任务，共166个变体）中集成到**ManiGaussian**框架。
- **对比方法**：
  - 物体识别：对比**Point-BERT**、**Point-MAE**、**Gaussian-MAE**及其不同SH阶数变体。
  - 世界建模：对比**PerAct**、**GNFactor**、**ManiGaussian**等。
- **评估设置**：
  - 三组姿态分布：ID/ID（标准姿态）、ID/SE(3)（零样本变换）、SE(3)/SE(3)（姿态增强训练+测试）。
  - 数据效率实验：在ModelNet10上限制训练子集比例（10%, 30%, 50%, 100%）。
  - SH消融：SH(0) vs SH(0,1) vs SH(0,1,2)。

### 4. 资源与算力
- **论文明确说明**：
  - 预训练与下游微调采用**NVIDIA H100 (Grace Hopper GH200) 96GB GPU**（附录F.5，表4）。
  - 世界建模实验使用**单张RTX 4090 GPU**，训练100k次迭代（附录H.3）。
- **未详细说明**：具体训练时长（epoch数）未给出，但预训练和微调均为300 epoch。

### 5. 实验数量与充分性
- **实验数量**：涵盖多个任务（分类、分割、机器人操作）、多个数据集（ModelNet10/40、ShapeNet Part、RLBench）、多组消融（SH阶数、数据比例、姿态变换）、以及定量和定性分析。
- **充分性与公平性**：
  - 所有对比方法使用**相同训练协议、epoch数、架构容量**（表1注）。
  - 分割实验中明确说明“keep the transfer configuration fixed across methods”。
  - 提供了Chamfer距离、不变描述符稳定性、动态损失等定量分析。
  - 缺点：部分实验（如RLBench）对比方法来源不同，但保持一致的评估protocol。
- **结论**：实验设计较充分，结果客观支持等变表示带来的鲁棒性和数据效率优势。

### 6. 主要结论与发现
- **零样本鲁棒性**：在ID/SE(3)设置下，E3DGS三类分类精度保持在~95%，而Gaussian-MAE从94%暴跌至18%，证明等变结构消除对姿态的过拟合。
- **数据效率**：在10%训练数据下，E3DGS仍显著优于基线（图4）。
- **高阶SH利用**：E3DGS能成功融入更高阶SEM（ℓ=1,2）提升性能，而基线加入更高阶SH反而导致零样本性能进一步下降（18%→12%）。
- **世界建模**：在RLBench上平均成功率50.4% vs 44.8%；在精确几何任务（如put in drawer +20%，sweep to dustpan +16%）中显著提升；动态损失收敛更快。
- **分割任务**：E3DGS在引入SH(0,1,2)后仍保持姿态一致性（80.81% mIoUC），而基线从82.86%降至77.67%（ID/ID）和29.52%（SE(3)）。

### 7. 优点
- **创新性**：首次将视角依赖的外观（球谐系数）**视为等变几何张量**，通过显式等变映射（Φ≤2）统一到矩阵载波，实现“颜色即几何”。
- **高效性**：避免昂贵的Clebsch–Gordan张量积，通过矩阵原生操作（共轭、Killing形式）实现等变，**参数效率高**（参数减少2.4–2.6倍）。
- **鲁棒性**：对任意SE(3)姿态变化保持精确等变，无需数据增强，在零样本变换下性能稳定。
- **可扩展性**：理论证明（Theorem 2）该方案可推广到任意球谐阶数（通过更大载波End(V_k)），当前3×3载波是ℓ≤2的特例。

### 8. 不足与局限
- **理论覆盖范围**：当前实现仅支持**ℓ ≤ 2**（最大带宽由3×3载波限制），不包含原始3DGS中可能使用的更高阶（如ℓ=3）。论文指出更高阶可通过更大载波处理，但未做实验验证。
- **计算开销**：尽管参数减少，但矩阵运算（Killing形式、共轭）导致**GMACs显著增加**（约7.5倍于基线，见表4），在实时应用中可能存在瓶颈。
- **标准姿态性能**：在规范姿态（ID/ID）下，E3DGS在ShapeNet Part分割上略低于使用SH(0)的基线（80.81 vs 82.86），表明等变性可能牺牲了利用姿态偏置的能力。
- **实验覆盖**：世界建模仅测试10个RLBench任务，未涵盖更复杂的动态场景或接触密集任务；且结果中部分任务（close jar, stack blocks）弱于基线，说明等变性在长程/接触任务中不一定总是最优。
- **资源表述不完整**：未提供完整训练时长（小时数），仅给出GPU型号和迭代数。

（完）
