---
title: "DPNeXt: A Lightweight Multi-Scale Feature Fusion Framework for Efficient ViT-Based Multi-Task Dense Prediction"
title_zh: DPNeXt：一种轻量级多尺度特征融合框架，用于基于ViT的高效多任务密集预测
authors: "Jehun Kang, Jungha Wang, Youngjun Hwang, David Hyunchul Shim"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16012v1"
tags: ["query:world-model"]
score: 8.0
evidence: 多任务密集预测用于3D空间场景理解
tldr: "多任务学习在机器人感知中需同时处理语义分割和深度估计，现有解码策略成为瓶颈。DPNeXt采用双深度可分离倒瓶颈结构实现高效特征融合解码，并提出多任务边界引导策略避免负迁移。在Cityscapes和NYUv2上达到最优性能，参数比标准DPT减少78.6%，推理速度在资源受限设备上最快。该工作为轻量级密集预测提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16012v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1832, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 897, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1751, \"height\": 1917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 463, \"label\": \"Table\"}]"
motivation: 现有密集预测解码策略效率低，且多任务间存在负迁移，需要轻量且解耦的解码方法。
method: 设计双深度可分离倒瓶颈解码器，并引入对称边界监督的多任务边界引导策略。
result: "Cityscapes上DPNeXt-B综合最优，NYUv2上分割与深度均最佳，参数减少78.6%，推理速度最快。"
conclusion: DPNeXt是高效替代标准DPT的轻量级多任务密集预测框架，兼顾性能与效率。
---

## 摘要
机器人感知系统中的多任务学习通过整合语义分割和深度估计，支持全面的3D空间场景理解。尽管视觉基础模型作为鲁棒的特征编码器被越来越多地采用，但现有的解码策略存在关键瓶颈。为此，我们提出DPNeXt，一种简化的多尺度特征融合解码器，作为标准密集预测变压器的高效替代方案。DPNeXt采用双深度可分离倒置瓶颈，通过以融合为中心的解码和独立任务模块化，改进对冻结视觉基础模型的利用。为进一步缓解任务间的负向归纳迁移，我们引入了多任务边界引导策略。与先前添加融合模块或门控的边界感知方法不同，多任务边界引导采用对称的边界聚焦监督，以鼓励几何一致性，无需额外注释或推理成本。在Cityscapes上的实验表明，DPNeXt-S优于先前最先进的多任务学习模型，而DPNeXt-B进一步提升了整体性能，在比较方法中取得了最佳结果。在NYUv2上，DPNeXt-B也在比较方法中实现了最佳的语义分割和深度估计结果，同时所需的可训练参数远少于先前的大规模多任务学习模型。与标准密集预测变压器相比，DPNeXt-S减少了78.6%的可训练参数，并在资源受限的笔记本电脑硬件上实现了比较模型中最快的推理速度。源代码、模型检查点和演示视频将在https://github.com/kangjehun/DPNeXt上提供。

## Abstract
Multi-Task Learning (MTL) in robotics perception systems supports comprehensive 3D spatial scene understanding by integrating semantic segmentation and depth estimation. While Vision Foundation Models (VFMs) are increasingly adopted as robust feature encoders, existing decoding strategies present a critical bottleneck. To address this, we propose DPNeXt, a streamlined multi-scale feature fusion decoder and efficient alternative to the standard Dense Prediction Transformer (DPT). DPNeXt uses dual depthwise separable inverted bottlenecks to improve frozen VFM utilization through fusion-centric decoding and independent task modularization. To further mitigate negative inductive transfer between tasks, we introduce the Multi-Task Boundary Guidance (MTBG) strategy. Unlike prior boundary-aware methods that add fusion modules or gating, MTBG applies symmetric boundary-focused supervision to encourage geometric consistency without extra annotation or inference cost. Experiments on Cityscapes show that DPNeXt-S outperforms prior state-of-the-art (SOTA) MTL models, while DPNeXt-B further improves the overall performance and achieves the best results among the compared methods. On NYUv2, DPNeXt-B also achieves the best semantic segmentation and depth estimation results among the compared methods while requiring substantially fewer trainable parameters than prior large-scale MTL models. Compared with the standard DPT, DPNeXt-S reduces trainable parameters by 78.6% and achieves the fastest inference speed among the compared models on resource-constrained laptop hardware. The source code, model checkpoints, and a demo video will be made available at https://github.com/kangjehun/DPNeXt.

---

## 论文详细总结（自动生成）

# DPNeXt: 轻量级多尺度特征融合框架用于高效ViT多任务密集预测——详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：机器人感知系统需要同时执行语义分割和深度估计等多任务（多任务学习，MTL），以实现3D空间场景理解。视觉基础模型（VFM，如DINOv2）作为强大的特征编码器被广泛采用，但现有的解码策略成为关键瓶颈：标准Dense Prediction Transformer（DPT）参数多、计算量大；而许多现代MTL模型（如InvPT++、TaskPrompter、MTMamba等）在引入复杂注意力或扫描操作后，理论FLOPs虽低，但实际硬件推理速度慢，且存在任务间负迁移。
- **核心问题**：如何在保持高精度的同时，极大降低解码器的参数量和推理延迟，并缓解多任务间的负向归纳迁移，且不增加推理阶段的开销。
- **整体意义**：提出DPNeXt框架，通过轻量级纯卷积解码器（仅标准操作）和零推理成本的边界引导策略，在Cityscapes和NYUv2上达到SOTA，参数减少78.6%，推理速度在资源受限设备上最快，为机器人等嵌入式场景提供了实用方案。

## 2. 方法论：核心思想与关键技术细节

### 总体框架（三阶段）
1. **特征提取**：使用冻结的DINOv2-Reg backbone，从第3、6、9、12层提取四个尺度的各向同性特征token（分辨率 H/14 × W/14，通道数 d_backbone）。
2. **多尺度重装（DPNeXt解码器）**：
   - **IPA（各向同性投影适配器）**：将各层特征投影到统一融合维度 d_fusion=256，并通过不同缩放因子（s_k = 4,2,1,1/2）调整空间分辨率，避免人工通道扩张。
   - **DDSIF（双深度可分离倒置融合）模块**：从深到浅递归聚合特征。每个DDSIF包含**两个深度可分离卷积阶段**，第一阶段在融合维度做深度滤波+点卷积扩展（扩展比r=2），第二阶段在扩展空间做深度滤波+点卷积投影回融合维度。采用倒置瓶颈结构，参数量极少。每次融合后内置2×双线性上采样。
3. **任务特定预测**：共享融合特征P_1输入到完全独立的三个任务头（分割、深度、边界），最后双线性上采样到原图分辨率。

### MTBG（多任务边界引导）策略
- 动机：缓解任务间负迁移，且不增加推理开销。
- 方法：
  - **辅助边界损失**（L_bound）：对边界二值图使用动态加权交叉熵（正负样本权重按批次统计）。
  - **边界感知损失**：仅对边界区域施加基础损失：
    - BAS：边界感知分割损失（L_bas）
    - BAD：边界感知深度损失（L_bad）
  - 边界标签来源：若有直接边界标注则用，否则通过Canny边缘检测+膨胀从语义分割标签导出。**推理时丢弃边界头，零延迟**。

### 总损失函数
L_total = λ_seg L_seg + λ_depth L_depth + λ_bound L_bound + λ_bas L_bas + λ_bad L_bad（NYUv2额外加表面法线L1损失，推理时丢弃）。

- L_seg：像素级OHEM交叉熵（仅保留预测概率<0.9的硬像素）。
- L_depth：SiLog损失，并采用对数归一化深度（先clip，再对数映射到[0,1]输出，反变换后计算损失）。

## 3. 实验设计

### 数据集与基准
- **Cityscapes**：城市驾驶场景，2975训练/500验证，19类语义分割，深度用CREStereo生成（范围0.001~80.0m）。评估指标：mIoU（分割）、RMSE（深度）、JPS（联合性能分数：将mIoU归一化到[0,1]与1-RMSE/深度范围取平均）。
- **NYUv2**：室内RGB-D，795训练/654测试，40类语义分割，深度范围0.001~10.0m。增加表面法线作为辅助任务。

### 对比方法
- 包括PAD-Net、MTL、DenseMTL、3-ways、SwinMTL、DPT-DINOv2-S、M2H、InvPT、TaskPrompter、MTMamba、MLoRE、MTMamba++等。同时自己训练了强基线DPT-DINOv2-S（DINOv2-S + 标准DPT解码器）。

### 实验协议
- Cityscapes：训练用518×518随机裁剪，评估用滑动窗口（1024×2048，窗口518×518，重叠0.5，高斯融合，无测试增强）。
- NYUv2：训练随机裁剪，评估resize到448×576单次前向，无滑动窗口或增强。
- 主要模型训练400 epoch，batch size 8，AdamW优化器，学习率1e-3（线性warmup+多项式衰减）。消融实验用160 epoch，batch size 32，4×Titan RTX。

## 4. 资源与算力

- **主要基准训练**：使用单张NVIDIA RTX 3090 GPU。
- **消融实验**：使用四张NVIDIA Titan RTX GPU（工作站），batch size 32，160 epoch。
- **推理速度测量**：在NVIDIA RTX 2080 Laptop GPU（作为资源受限设备代理）上测量FPS。
- **训练时间**：未明确报告具体小时数，仅提及epoch数。

## 5. 实验数量与充分性

| 实验类型 | 数量 |
|-------|------|
| 主要结果（两个数据集） | 2组（Cityscapes表I、NYUv2表II） |
| 消融实验 | 1组（表IV：从DPT→IPA→DDSIF双深度→倒置瓶颈的逐步消融，以及MTBG各组件消融） |
| 计算效率对比 | 1组（表III：参数量、GFLOPs、FPS，与多个SOTA对比） |

- **充分性评估**：较充分。覆盖了主基准、计算效率、结构消融和MTBG组件消融。对比方法广泛（10+种），且对不可复现的基线标注了特殊情况（如M2H结果不一致，SwinMTL官方代码不可用）。但缺少以下潜在实验：（1）在更多数据集（如KITTI、ADE20K）上的泛化；（2）不同backbone（如Swin、ConvNeXt）替换实验；（3）更大规模的数据集和更细粒度的边界分析。
- **公平性**：作者尽量使用官方公开结果或自复现，并单独训练了DPT-DINOv2-S作为公平强基线。但部分对比方法因代码不可用只能引用论文数字，可能存在实现差异。

## 6. 主要结论与发现

- **DPNeXt-B在Cityscapes和NYUv2上均达到最佳JPS**（0.867和0.789），语义分割和深度估计均最优。
- **DPNeXt-S以极小的可训练参数（6.5M/6.8M）超越多数先前方法**，参数比标准DPT减少78.6%，且推理速度最快（51.02 FPS，RTX 2080 Laptop）。
- **MTBG有效缓解负迁移**：完整MTBG（BAS+BAD+辅助边界损失）相比仅OHEM提升JPS 0.004（0.810→0.814）。
- **DPNeXt的纯CNN设计在硬件上更友好**：虽GFLOPs中等，但实际FPS远超理论更优的自定义层方法（如MTMamba++、MLoRE）。

## 7. 优点

1. **极轻量化**：DPNeXt-S仅6.5M可训练参数（标准DPT需30.2M），且全部为标准卷积，无自定义算子，利于部署。
2. **零推理开销的MTBG**：训练时用边界损失正则化，推理时丢弃辅助头，不增加任何延迟。
3. **独立任务头**：无交叉任务依赖，避免结构耦合，便于扩展或丢弃。
4. **即插即用**：可替换标准DPT解码器，与冻结VFM配合简单。
5. **公平且详实的实验**：包含强基线DPT-DINOv2-S，并标注了无法复现的对比方法，透明性好。
6. **定量和定性分析结合**：可视化注意力图、预测结果对比，论证设计的合理性。

## 8. 不足与局限

1. **实验数据集有限**：仅两个数据集（Cityscapes、NYUv2），未在更多场景（如KITTI、ADE20K、室内外混合）验证泛化能力。
2. **缺少backbone消融**：仅使用了DINOv2-Reg，未测试其他VFM（如DINOv1、SAM、CLIP），无法确认DPNeXt的通用性。
3. **边界标签依赖**：当没有语义标签时，从分割掩码用Canny推导边界，可能引入噪声。未分析不同边界质量的影响。
4. **未研究动态部署**：虽强调了资源受限推理，但未在手机、边缘设备（如Jetson）上实测，仅用笔记本电脑GPU代理。
5. **多任务扩展性**：当前仅测试了分割+深度两个主要任务（NYUv2额外加法线），未在更多任务（如边缘检测、法线、表面检测）上验证。
6. **训练开销**：虽然推理快，但训练仍依赖多GPU（消融用4×Titan RTX），未报告收敛速度或训练成本。
7. **潜在偏差风险**：Cityscapes深度由CREStereo生成（伪标签），可能存在系统性偏差；未报告纯真实深度上的结果（如KITTI深度）。

（完）
