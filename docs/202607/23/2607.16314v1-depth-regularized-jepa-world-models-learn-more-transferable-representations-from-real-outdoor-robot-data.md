---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化的JEPA世界模型从真实户外机器人数据中学习更具迁移性的表示
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:world-model"]
score: 9.0
evidence: 深度正则化JEPA世界模型，户外机器人
tldr: "真实户外机器人视频数据视觉复杂度高，现有JEPA世界模型难以学习鲁棒潜在动力学。本文在训练中引入深度作为几何先验，并采用各向同性正则化SIGReg最大化任务无关的潜在多样性，同时增加训练时过参数化。在真实农业机器人视频上训练的18M参数模型，相比LeWM基线，视觉里程计误差降低33%，域内外惊喜检测分离显著提高，多步展开保真度改善且随步长增长。结果表明，轻量几何先验使紧凑世界模型在不增加推理开销下学到更可迁移的表示。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717, \"label\": \"Table\"}]"
motivation: 真实户外机器人视频数据视觉复杂，现有世界模型难以学习鲁棒且可迁移的潜在动力学表示。
method: 引入深度作为几何先验，结合各向同性正则化SIGReg和训练时过参数化，优化符合场景几何的高熵潜在表示。
result: "在农业机器人数据上，视觉里程计误差降低33%，域内外惊喜分离和多步展开保真度显著提升。"
conclusion: 轻量深度先验有效提升JEPA世界模型在真实户外数据上的迁移性与泛化能力，且推理无额外开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的模型，已被证明能够学习各种环境的鲁棒动态。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，特别是在不可预测的户外环境中。我们在训练过程中引入深度作为几何先验，以直接从机器人视频数据中学习更鲁棒的潜在动态，并处理视觉复杂性。这将深度监督与诱导各向同性的潜在正则化器（SIGReg）相结合，最大化任务无关的潜在多样性，同时约束多样性的组织方式，其综合目标针对与场景几何一致的最高熵表示。为了在不大幅增加推理时间的情况下满足这种更高的复杂性，我们还添加了仅训练时的过参数化。在真实农业机器人视频上训练一个18M参数的模型，我们通过冻结表示的视觉里程计探针、基于预测器的惊奇检测和多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探针误差降低了33%，显著提高了领域内和领域外TartanGround基准上的惊奇分数分离度，并在领域偏移下提高了多步展开保真度，其增益随展开步长增加而增加。值得注意的是，我们在与3D几何不直接相关的物理理解（如光照和阴影）上也看到了惊奇分数分离度的提升。这些结果表明，轻量级的训练时几何先验使紧凑的JEPA世界模型在具有强底层表示的真实户外数据上更加有用和更具迁移性，且不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：真实户外机器人数据（如农业机器人）面临视觉复杂性问题（变化光照、阴影、运动模糊、植被杂乱、地形变化等），现有的世界模型（尤其是基于JEPA架构的模型）在简单模拟环境中表现良好，但在真实户外数据上难以学习鲁棒的潜在动态，且容易陷入表示坍缩或过度拟合无关外观变异。
- **核心问题**：如何在训练过程中引入有效的几何先验，使紧凑的JEPA世界模型在视觉复杂的真实户外数据上学习到更具迁移性和物理意义的潜在表示，同时不增加推理阶段的计算开销。
- **整体含义**：通过训练时轻量级的深度正则化，可以引导潜在空间向符合场景几何结构的高熵表示发展，从而提升模型在运动估计、异常检测和多步预测等任务上的泛化能力。

## 2. 论文提出的方法论
- **核心思想**：在LeWorldModel（LeWM）的基础上，增加一个**训练时深度对齐损失**，将RGB嵌入与对应深度图像的嵌入（通过共享编码器-投影器网络）进行余弦相似度对齐，且深度分支使用stop-gradient，使RGB编码器学习几何结构信息。同时保留SIGReg正则化（各向同性高斯约束）最大化任务无关的潜在多样性，两者结合的目标是寻找**与场景几何一致的最高熵表示**。
- **关键技术细节**：
  - **基础架构**：基于LeWM，使用ViT-Tiny编码器（嵌入维度192），6层预测器，历史帧数3，预测1步。
  - **训练损失**：总损失 \( L = L_{\text{pred}} + 0.09 L_{\text{sigreg}} + 0.1 L_{\text{depth}} \)，其中 \( L_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})]) \)。
  - **训练时过参数化**：在预测器的MLP上支路和下支路上添加rank-16的线性分支（RepLinear），训练后可通过代数合并到原始权重，不影响推理速度。
  - **深度数据使用**：深度图截断至1米，灰度复制为3通道，作为输入传入相同编码器；仅在训练时使用，推理时只使用RGB。
- **算法流程**（文字描述）：给定RGB历史帧和动作，编码器-投影器输出当前嵌入，预测器生成下一帧潜在嵌入，计算预测损失；同时，当前帧RGB嵌入与深度嵌入（stop-gradient）计算余弦对齐损失；SIGReg约束嵌入统计量接近各向同性高斯。三个损失联合优化。

## 3. 实验设计
- **数据集**：
  - **内域数据**：Aigen Element农业机器人收集的约31K帧训练数据（85个episode），验证集3,587帧（9个episode），包含多样化户外场景，深度由立体相机提供。
  - **域外数据**：TartanGround基准（地面机器人仿真数据），15,608帧（6个episode），覆盖多种环境，引入sim-to-real领域偏移。
- **评估任务与Benchmark**：
  - **视觉里程计线性探针**：从冻结嵌入中回归位姿（相对平移/旋转），使用MSE指标。
  - **惊奇检测**：构造短片段（正常vs.合成扰动，包括遥传、时间反转、亮度跳变、颜色交换），计算预测误差并评估准确率和平均惊奇分数分离度。
  - **多步潜在展开保真度**：在真实动作序列下自回归展开预测（步长h=10和h=20），计算目标相似度和轨迹相似度（余弦相似度）。
- **对比方法**：
  - **Vanilla LeWM**（无深度正则化、无过参数化）作为主要基线。
  - **Frozen DINOv2**编码器作为非世界模型参考（仅用于VO探针和展开对比）。
- **消融实验**：论文未单独隔离过参数化的贡献（仅提及在讨论部分留作未来工作）；缺少无SIGReg的深度损失消融。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量及训练时长。仅提到模型参数量为18M，训练100个epochs，采用相同的优化器和调度策略。未提供具体的训练时间或硬件配置信息。

## 5. 实验数量与充分性
- **实验数量**：主要报告了三类评估（VO探针、惊奇检测、多步展开），并在内域和域外数据集上分别进行，共生成约6组独立数值结果（表1）。此外还包括跨域潜在几何分析（图3）。
- **充分性**：
  - **优点**：评估维度覆盖了表示质量、预测行为泛化和迁移性，且包含内域和域外对比。
  - **不足**：
    - 仅与一个基线（Vanilla LeWM）进行直接对比，缺少与其他JEPA变体（如V-JEPA、DINO-WM）或更大规模模型的比较。
    - 未进行多组随机种子重复报告（提及“重复训练得到一致结果，报告代表性运行”），但未提供标准差或置信区间。
    - 缺乏对深度权重和过参数化rank的敏感性分析。
    - 缺少消融：未评估仅SIGReg、仅深度损失、无过参数化等情况的影响。
- **客观性与公平性**：实验设置基本公平，但内域展开中深度模型略低于基线，域外才反转，作者对此有合理讨论；VO探针和惊奇分离度上的提升明显。

## 6. 论文的主要结论与发现
1. **表示质量提升**：冻结嵌入的VO线性探针MSE降低33%（0.0022→0.0015），优于Frozen DINOv2（0.0029）。
2. **惊奇分离增强**：内域平均分离度从0.067提升至0.120，域外（TartanGround）从0.035提升至0.099（近3倍提升），且准确率在多数扰动上饱和但分离度显著增加。
3. **非几何物理理解改善**：亮度跳变和颜色交换扰动下的惊奇分离度在内域和域外均有大幅提升（如内域亮度跳变从0.114→0.247），表明深度先验帮助建立了外观与几何结构的约束关系。
4. **域外多步展开优势**：在域外数据上，深度模型的目标相似度在h=10时提升4%，在h=20时提升17%（0.458→0.537），且增益随步长增加；内域略低于基线，体现trade-off。
5. **跨域潜在几何对齐**：深度正则化模型的两个域之间主成分子空间重叠更高（k≈5-50），分离度降低（centroid距离从9.96σ降至8.96σ），证明表示更具迁移性。

## 7. 优点
- **方法轻量有效**：仅训练时引入深度对齐损失和过参数化，推理时无任何额外开销，适合资源受限的机器人部署。
- **结合最大熵与几何约束**：SIGReg提供抗坍缩保证，深度损失提供物理意义导向，两者协同优于纯各向同性。
- **评估丰富**：同时使用探针、惊奇检测和展开预测，覆盖表示质量、预测行为和动态泛化。
- **域外验证有力**：从真实农业数据到仿真地面机器人数据的领域偏移较大，结果提升显著，表明方法的泛化价值。
- **发现非直觉结果**：深度先验也提升了外观相关扰动的分离度，揭示了几何结构对物理理解的整体增强作用。

## 8. 不足与局限
- **规模与数据范围窄**：训练仅约31K帧，来自单一机器人平台和单一农业场景，在更大规模和更多样领域下是否成立未验证。
- **基线对比有限**：仅与LeWM自身对比，未与更先进的世界模型（如V-JEPA 2、Dreamer）或大规模预训练模型比较；DINOv2仅作为参考，不是公平对比对象。
- **离线指标而非闭环性能**：多步展开度量是在真实动作序列下的潜在空间相似度，而非闭环机器人规划或控制性能，实际应用价值待验证。
- **缺少充分消融**：未独立分析深度损失权重、过参数化rank、SIGReg强度的影响；未区分过参数化与深度损失的各自贡献。
- **计算资源未报告**：无法评估方法的实际训练成本。
- **实验重复性不足**：仅报告“代表性运行”，未提供多次运行的标准差，统计显著性不确定。
- **规划评估缺失**：作者尝试初步采样规划但无结论，模型在规划任务中的实际表现未知。
- **潜在偏差**：深度损失权重0.1为启发式选择，未进行系统调参；训练数据中深度截断1米可能丢失远场几何信息。

（完）
