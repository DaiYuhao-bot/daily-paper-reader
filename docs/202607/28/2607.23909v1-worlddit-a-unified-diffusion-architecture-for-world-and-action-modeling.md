---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: "WorldDiT: 一种用于世界与动作建模的统一扩散架构"
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:world-model"]
score: 9.0
evidence: 统一扩散架构用于世界建模与动作生成
tldr: 现有机器人策略常依赖大型预训练视觉语言模型（VLM）作为动作骨干，但参数量大、训练成本高。WorldDiT提出一种统一的扩散变换器架构，无需VLM即可同时生成连续动作块并预测未来相机帧的归一化RGB patch目标。在四个LIBERO模拟套件上，WorldDiT在总模型参数与平均成功率方面均位于帕累托前沿。该工作为未来扩展研究提供了一个低于十亿参数的强劲基线，证明了扩散变换器在联合世界建模与动作生成中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有依赖大型VLM的动作骨干参数量大，WorldDiT旨在无需预训练VLM即可高效联合建模世界与动作。
method: 采用单一扩散变换器同时生成连续动作块并预测未来相机帧的归一化RGB patch。
result: 在四个LIBERO套件上，位于参数-成功率帕累托前沿，提供低于十亿参数的强基线。
conclusion: 证明了无需大型预训练VLM，扩散变换器即可实现强力的世界与动作联合建模。
---

## 摘要
许多最近的机器人策略通过使用大型预训练视觉-语言模型（VLM）作为动作主干来追求更强的控制。我们提出了WorldDiT，一种统一扩散Transformer架构，它将动作生成与视觉世界建模相结合，无需大型预训练VLM动作主干即可实现强性能。在训练过程中，单个扩散Transformer生成连续动作块，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO模拟套件中，WorldDiT位于报告的总模型参数和平均成功率之间的帕累托前沿上，这些结果均为报告所有四个套件的方法。这些结果为未来的缩放研究提供了一个强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# 论文总结：WorldDiT: 一种用于世界与动作建模的统一扩散架构

## 1. 核心问题与整体含义

- **研究动机**：当前许多机器人策略依赖大型预训练视觉-语言模型（VLM）作为动作骨干，虽然带来了强感知和语言理解能力，但也使得“架构贡献”与“规模贡献”难以分离；同时，依赖大模型导致参数量巨大（数十亿），训练和部署成本高。
- **核心问题**：**是否可以在不使用大型预训练VLM作为动作骨干的情况下，通过统一的扩散Transformer架构，将连续动作生成与未来视觉预测相结合，并保持强控制性能？**
- **整体含义**：WorldDiT旨在探索一种更轻量、更紧凑的范式——以不足十亿参数（399M）的扩散Transformer，同时完成世界建模（预测未来RGB patch）和动作生成，并在LIBERO基准上达到与依赖大VLM的方法相当甚至更优的性能，为未来扩展研究提供基线。

## 2. 方法论

- **核心思想**：使用单一扩散Transformer骨干，在训练时联合学习两个目标——连续动作块（7步）和未来相机帧的归一化RGB patch（来自主摄像头和腕部摄像头）。共享骨干通过流匹配（flow matching）同时预测动作和RGB patch的速度场。推理时，只生成动作，RGB patch分支被移除。
- **关键技术细节**：
  - **输入编码**：冻结的视觉编码器（MAE）和文本编码器（CLIP）提取视觉和语言特征；可训练的机器人状态编码器处理关节/夹爪状态；所有特征通过可训练投影映射到骨干隐藏维度。
  - **窗口结构**：10步轨迹窗口，前3步作为观测上下文（C=3），后7步作为动作目标（H=7），第10步（即偏移H=7后的帧）提供未来RGB patch目标。
  - **RGB patch目标**：将224×224 RGB图像划分为16×16 patch，每个patch向量为768维，经归一化后从每个摄像头均匀选取64个patch（共128个）。仅在最后一个时间槽施加RGB patch损失。
  - **流匹配训练**：对动作目标y和RGB patch目标分别添加高斯噪声，定义xτ = (1-τ)ε + τy，训练模型预测速度(y-ε)。总损失为动作速度损失和RGB patch速度损失的加权和（权重分别为0.1和0.001）。
  - **推理**：从高斯噪声开始，通过20步欧拉积分生成7步动作块，执行前3个动作后重新规划（滚动时域控制），并使用时间集成（temporal ensembling）处理重叠动作。
- **公式/算法流程**：文中给出了流匹配损失公式L_flow = E[||v_θ(xτ,τ,c) - (y-ε)||²]，以及总损失L_total = w_action * L_action_flow + w_rgb * L_rgb_flow。

## 3. 实验设计

- **数据集/场景**：使用LIBERO操作基准中的四个下游套件：`libero_spatial`、`libero_object`、`libero_goal`、`libero_10`（报告中称为LIBERO Long）。还使用多任务`libero_90`分割进行预训练。
- **Benchmark**：每个套件报告500个模拟episode的成功率，以及四个套件的平均成功率。与24种方法对比，包括基于大VLM的方法（如ACoT VLA、π0.5、OpenVLA等）和不依赖大VLM的方法（如DreamVLA、FlowVLA等）。
- **对比方法**：共24个方法，分为两组（大型预训练VLM动作骨干 vs 无大型预训练VLM动作骨干）。Seer仅报告Long分数，未纳入帕累托计算。
- **评价指标**：总模型参数（含推理必需的冻结模块）和平均成功率。

## 4. 资源与算力

- **训练硬件**：单节点配备8块RTX Pro 6000 GPU。
- **预训练**：在libero_90上预训练30个epoch，per-GPU batch size=40，梯度累积2步，总有效batch size=80（？原文写per-GPU batch size=40, gradient accumulation=2，但未明确总batch size）。学习率1e-4，余弦调度，1个warmup epoch。
- **微调**：对每个下游套件单独微调，per-GPU batch size=32，8块GPU，2步梯度累积，有效batch size=512。使用bf16混合精度。
- **总参数量**：399.084M总参数（含冻结），135.107M可训练参数。
- **说明**：训练时长未明确给出。

## 5. 实验数量与充分性

- **实验数量**：
  - 在4个LIBERO套件上各评估500个episode（共2000 episode）。
  - 每个套件的评估包含300个用于阶段性checkpoint选择的episode，因此文中声明报告的平均成功率（94.9%）并非完全独立的测试估计。
  - 未进行消融实验（如移除RGB patch预测、改变窗口大小、调整损失权重等）。
  - 仅报告一种配置（depth=4, hidden=1024, heads=16, register tokens=4, C=3, H=7）。
- **充分性评价**：
  - **优点**：对比了24种方法，覆盖了当前主流方法；在参数-成功率帕累托前沿上定位，提供了可复现的基线。
  - **不足**：
    - 仅使用单一配置，未探索不同规模或架构变体。
    - 无消融实验验证RGB patch预测的贡献。
    - 评估结果包含用于checkpoint选择的episode，可能存在过拟合偏差，文中明确承认“不应视为无偏测试估计”。
    - 对比方法的数据来自各自原始报告，而非统一复现，存在协议不一致的风险。
    - 仅在一个仿真基准（LIBERO）上评估，未在真实机器人或更复杂环境中验证。

## 6. 主要结论与发现

- **主要结论**：WorldDiT（399M参数）在LIBERO四个套件上平均成功率为94.9%，位于参数-成功率帕累托前沿上。这意味着：在不使用大型VLM动作骨干的前提下，通过统一的扩散Transformer联合世界建模与动作生成，可以取得与依赖数十亿参数VLM的方法相竞争甚至更优的结果。
- **具体结果**：Spatial 98.0%, Object 97.0%, Goal 92.8%, Long 91.8%。Long仍是最难的套件。
- **发现**：对于报告所有四个套件的方法，所有比WorldDiT成功率更高的方法使用了更多总参数，而参数量相同或更少的方法成功率更低。这刻画了亚十亿参数规模下的性能-参数权衡。

## 7. 优点

- **架构创新**：首次将扩散Transformer同时用于连续动作生成和未来RGB patch预测，且推理时无需视觉预测分支，保持轻量。
- **无需大型VLM骨干**：通过共享可训练骨干和冻结的轻量编码器，显著降低参数量和训练成本。
- **帕累托最优性**：在参数-成功率图上位于前沿，为资源受限场景提供了可行方案。
- **可扩展性**：基于流匹配的统一公式允许未来分解为独立专家（如Paris架构），适用于异构计算平台。
- **透明性**：明确区分了冻结与可训练参数，并公开了资源配置和训练协议。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅在一个仿真基准（LIBERO）上评估，未涉及真实机器人、多样物体或动态场景。
  - 缺少消融实验（如移除RGB patch目标的影响、不同窗口长度/噪声步数等）。
  - 仅测试一种模型配置（深度4，隐藏1024），未探索更大或更小的规模。
- **评估偏差风险**：
  - 报告的成功率包含用于checkpoint选择的300/500 episode，并非严格独立测试，可能高估真实泛化性能。
  - 对比方法使用各自原始报告数据，而非统一复现，实验协议（如种子、episode数、评估细节）可能存在差异，影响公平性。
- **推理依赖**：虽然推理时去掉了RGB分支，但训练仍需未来帧作为监督，可能依赖完美仿真环境；在真实世界中获取未来帧不可行，限制了其直接迁移。
- **未开源权重与代码**：文中指出“不完整的公开可用性”，阻碍第三方复现与公平对比。
- **泛化性未验证**：仅针对LIBERO特定任务（桌面操作），未说明对复杂长时序任务或跨具身平台的表现。
- **时间与计算成本**：未报告完整训练时间，使得资源估算困难。

（完）
