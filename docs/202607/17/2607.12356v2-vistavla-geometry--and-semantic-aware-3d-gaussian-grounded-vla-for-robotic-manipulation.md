---
title: "VistaVLA: Geometry- and Semantic-Aware 3D Gaussian-Grounded VLA for Robotic Manipulation"
title_zh: "VistaVLA: 几何与语义感知的三维高斯基础VLA用于机器人操作"
authors: "Mohan Liu, Zhihao Gu, Xuanyu Chen, Haitian Zhang, Kaimin Mao, Yan Wu, Wei-Yun Yau, Lin Wang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12356v2"
tags: ["query:world-model"]
score: 8.0
evidence: 3D语义认知地图作为机器人操作的世界模型
tldr: "现有视觉-语言-动作模型缺乏显式3D表示，难以进行空间布局和几何约束推理。本文提出VistaVLA，先通过3D高斯原语构建几何与语义融合的3D认知表示，再用Merge-then-Query机制将稠密原语压缩为紧凑令牌（减少99%），实现高效策略学习。在真实环境中，成功率平均提升22.8%，挑战性分布外任务提升30.0%。该方法首次将显式3D语义认知地图引入VLA，显著增强空间推理与操控性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12356v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12356v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12356v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1377, \"height\": 771, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12356v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1415, \"height\": 523, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12356v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 719, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12356v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 704, \"height\": 365, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12356v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 712, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12356v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 547, \"height\": 268, \"label\": \"Table\"}]"
motivation: 现有VLA模型缺少场景级3D语义认知表示，难以理解空间布局和几何约束，限制操控性能。
method: VistaVLA将多视图视觉语言特征提升为3D高斯原语，形成几何锚定语义令牌，再通过MtQ压缩成紧凑空间令牌供策略学习。
result: "真实世界7个任务成功率平均提升22.8%，分布外任务比VLA-Adapter基线提升30.0%。"
conclusion: 融合几何与语义的显式3D认知表示能有效增强VLA模型的空间推理与操控能力。
---

## 摘要
视觉-语言-动作（VLA）模型通过将语言指令和二维视觉输入直接映射到动作，已成为机器人操作中一种强大的端到端范式。然而，这些模型缺乏明确的场景级三维表示，限制了它们对空间布局和几何约束的推理能力。虽然最近的工作引入了显式的三维线索（如深度图或点云）以增强几何感知，但它们主要捕捉低级结构，并且在三维空间中缺乏高级语义基础。在人类认知中，与物理世界的交互依赖于三维语义认知地图——一种将空间布局与语义上下文整合的内部心理模型，从而实现持久、视角不变的推理。鉴于此，我们提出了VistaVLA，一种新颖的两阶段框架，该框架从三维高斯基元构建几何和语义感知的三维认知表示，并将其作为紧凑的上下文令牌用于VLA策略学习。具体而言，VistaVLA将多视图视觉-语言特征提升到三维高斯基元中，形成几何锚定的语义令牌，这些令牌将视图一致的空间基础与二维视觉特征空间对齐。为了使这种三维表示在计算上易于处理以实现有效的VLA控制，我们引入了先合并后查询（MtQ）令牌总结机制。MtQ将密集的高斯基元压缩为一组高度紧凑的空间信息令牌，在保留动作相关的三维布局和语义上下文的同时，实现了99%的令牌减少。在模拟和真实环境中的广泛评估证明了VistaVLA的有效性。值得注意的是，在真实场景中，VistaVLA在七个真实世界任务上将成功率提高了22.8%，并在具有挑战性的分布外任务上比VLA-Adapter基线提高了30.0%。

## Abstract
Vision-Language-Action (VLA) models have emerged as a powerful end-to-end paradigm for robotic manipulation by mapping language instructions and 2D visual inputs directly to actions. However, these models lack an explicit, scene-level 3D representation, limiting their ability to reason over spatial layouts and geometric constraints. While recent efforts incorporate explicit 3D cues, such as depth maps or point clouds, to improve geometric awareness, they primarily capture low-level structures and lack high-level semantic grounding in 3D space. In human cognition, interaction with the physical world relies on a 3D semantic cognitive map - an internal mental model that integrates spatial layouts with semantic context to enable persistent, viewpoint-invariant reasoning. In light of this, we present VistaVLA, a novel two-stage framework that constructs a geometry- and semantics-aware 3D cognitive representation from 3D Gaussian primitives and grounds it as compact context tokens for VLA policy learning. Specifically, VistaVLA lifts multi-view vision-language features into 3D Gaussian primitives, forming geometry-anchored semantic tokens that align view-consistent spatial grounding with 2D visual feature spaces. To make this 3D representation computationally tractable for effective VLA control, we introduce Merge-then-Query (MtQ), a token summarization mechanism. MtQ compresses dense Gaussian primitives into a highly compact set of spatially informative tokens, achieving a 99% token reduction while preserving action-relevant 3D layouts and semantic context. Extensive evaluations in both simulated and real-world environments demonstrate the effectiveness of VistaVLA. Notably, in real-world scenarios, VistaVLA improves success rates by 22.8% across seven real-world tasks and by 30.0% over the VLA-Adapter baseline on challenging out-of-distribution tasks.

---

## 论文详细总结（自动生成）

# VistaVLA 论文详细总结

## 1. 论文的核心问题与整体含义

- **核心问题**：现有的视觉‑语言‑动作模型主要依赖二维图像输入，缺乏显式的场景级三维表示，导致在空间布局和几何约束推理上能力不足。虽然部分工作引入深度图或点云等三维线索，但只提供低级几何结构，缺少在三维空间中与高级语义（如语言对齐的特征）的融合，限制了复杂操作任务的成功率。
- **整体含义**：受人类认知中“三维语义认知地图”的启发，作者提出一种能同时感知几何结构与语义信息的显式三维场景表示，并将它作为紧凑的上下文令牌注入VLA策略网络，从而提升机器人在空间推理、视角不变性和泛化方面的能力。

## 2. 论文提出的方法论

### 核心思想
构建一个**几何与语义联合的三维高斯场景表示**（3D Gaussian-Grounded Cognitive Representation），将其压缩为少量紧凑令牌，作为VLA策略的额外3D上下文输入。

### 关键技术细节（两阶段）

#### 阶段 I：三维高斯基元语义场构建
1. **紧凑教师特征空间**：使用SigLIP2和DINOv2-Large提取每帧的密集视觉特征（共2176维），通过轻量自编码器压缩为128维潜码，作为教师特征。
2. **两阶段高斯场训练**：
   - 基于DepthSplat初始化高斯原语 `g_i = (μ_i, Σ_i, α_i, c_i, z_i)`，其中 `z_i` 为可学习语义特征。
   - **阶段1**：仅用RGB‑D监督（`L_stage1 = λ_rgb L_rgb + λ_dep L_dep`）稳定几何。
   - **阶段2**：加入教师特征监督（`L_stage2 = λ_rgb L_rgb + λ_dep L_dep + λ_feat L_feat`），通过体积渲染（如公式(1)所示）将2D语义特征蒸馏到每个高斯原语的 `z_i` 上，实现多视图一致的3D语义锚定。
3. **在线构建**：推理时，利用当前RGB观测和标定相机位姿在线构建高斯场，无需预建地图，可捕捉实时场景变化。

#### 阶段 II：Merge‑then‑Query（MtQ）令牌压缩与策略注入
1. **参数免费压缩**：
   - 将稠密高斯令牌（约10⁵个）先通过步长4的平均池化降采样到约7K。
   - 再基于莫顿码排序和语义相似度匹配进行迭代合并：划分锚点集与非锚点集，只合并相似度前50%的配对，通过大小加权平均更新锚点特征、中心、聚合大小。重复直到令牌数 ≤ N'（设为1000）。
2. **查询式摘要**：
   - 使用 `N_q = 64` 个可学习查询令牌，通过两层Transformer解码器对压缩后的Z'令牌进行交叉注意力，输出 `X_gs ∈ R^{B×64×d_llm`。
3. **策略注入**：
   - 将 `X_gs` 与2D视觉令牌、语言指令令牌、动作查询令牌拼接为 `X = [X_vis, X_lang, X_gs, X_act]`，输入LLM（0.5B）进行因果序列建模。
   - 动作头：预测未来K个连续动作（执行前4个后重新规划），使用L1回归损失。

### 公式说明
- **渲染特征**：`F_v(p) = Σ_{i∈N(p)} R_i(v,p) z_i`，其中 `R_i` 由高斯投影、透明度、深度顺序和相机参数决定。
- **特征蒸馏损失**：`L_feat = Σ_v Σ_p ||F_v(p) - F_teacher(p)||²`。
- **动作损失**：`L_act = Σ_{t=1}^K ||a_t - a_t||₁`。

## 3. 实验设计

### 数据集 / 场景
- **真实世界**：在Agile‑Piper平台上构建7个桌面操作任务（如PlaceSponge、StackBoxes、StackBowls、PlacePlate、OrganizeSponge、ThrowTrash、Wand&Cup），每个任务10次试验。
- **模拟环境**：标准LIBERO基准（Spatial、Object、Goal、Long四个子集）以及LIBERO‑Pro‑Swap（空间分布外场景）。

### 对比方法
- **基线**：VLA‑Adapter‑0.5B（主要基线）、VLA‑Adapter+Depth（深度增强版）、SmolVLA‑0.5B、π0.5‑3B、OpenVLA、UniVLA、π0等。
- 所有方法在相同演示数据、相同固定相机视图下训练/微调。

## 4. 资源与算力

- 论文明确提到：**所有VLA方法均在4块RTX 5090 GPU上训练/微调**，训练总预算与VLA‑Adapter保持匹配。具体训练时长未说明（包括每个epoch时间、总迭代次数等）。

## 5. 实验数量与充分性

### 实验组数
- **真实世界**：7个任务，每个10次试验，共70次试验；另外在2个任务上测试空间扰动（深度/位置变化），各10次。
- **模拟**：标准LIBERO四个子集 + LIBERO‑Pro‑Swap场景；此外在LIBERO‑Pro‑Swap上报告了多个基线的结果。
- **消融实验**：三大类共6~7组消融，涵盖：
  - 相机/令牌数量消融（3组+本文方法）。
  - 压缩模块消融（GS+FPS+Dec.、GS+Entropy+Dec.、GS+Merge+Linear、全文）。
  - 查询令牌数量消融（16/32/64/128/256）。
- 总计约 **20+ 组对比实验**（含不同基线和消融）。

### 充分性评价
- **公平性**：所有基线使用相同的固定视图数量、相同训练预算，基线也做了相机视图增补对比，控制变量较好。
- **客观性**：真实环境采用固定的初始姿态和恢复物体位置，减少随机干扰，且报告成功次数；模拟使用了标准评测流程。
- **覆盖度**：同时覆盖真实和模拟，涵盖简单和复杂空间变化任务，且有专门的空间泛化测试。消融覆盖了方法的关键组件（压缩、查询、输入视图）。总体比较充分。

## 6. 论文的主要结论与发现

1. **VistaVLA显著提升操作成功率**：真实世界7个任务平均成功率比VLA‑Adapter提升22.8%，在分布外场景（深度扰动）提升30.0%。
2. **空间泛化增强**：在深度/位置扰动测试中，VisatVLA是唯一在位置扰动下有成功（3/10）的方法，深度扰动成功率达9/10。
3. **模拟性能领先**：标准LIBERO平均96.05%（无大规模预训练），LIBERO‑Pro‑Swap达到12.2%（基线仅1.7%）。
4. **令牌压缩有效**：MtQ实现99%令牌压缩（10⁵→64）仍保留动作相关3D信息。
5. **语义‑几何联合表示优于纯几何或纯2D**：对比深度增强基线，发现单纯加入深度提升有限，而语义高斯表示提供更连贯的空间上下文。

## 7. 优点

- **创新性**：首次将3D语义高斯场作为显式认知表示引入VLA策略，模仿人类认知地图。
- **优雅的压缩设计**：MtQ结合莫顿排序、相似度合并与查询摘要，实现高效降维而不失关键信息。
- **实用性强**：在线构建高斯场，无需预建地图，适应实时场景变化；推理时只需RGB输入和标定位姿。
- **实验充分**：同时覆盖真实与模拟，包含多种基线对比、空间分布内外测试、详细消融，结论可靠。
- **跨任务泛化表现突出**：即使在低数据量下（单任务100+演示），仍能大幅超越强基线（如π0.5）。

## 8. 不足与局限

- **实验覆盖有限**：仅局限于桌面操作、固定机器人平台（Agile‑Piper）和标定多视角相机，未涉及移动操作、灵巧手抓取、动态遮挡、大范围障碍物等。
- **相机位姿依赖**：构建高斯场需要标定过的相机位姿，在位姿估计不可靠时可能失效。
- **计算实时性**：虽然MtQ实现了令牌压缩，但未明确报告在线构建高斯场和MtQ的推理耗时（如FPS），实际实时性存疑。
- **消融实验局限**：消融仅在少数任务上执行（如T3），可能未完全反映在所有任务上的影响。
- **未探明扩展性**：仅在0.5B规模LLM上测试，更大模型是否收益更大未知。
- **分布外挑战仍较大**：位置扰动成功率仅3/10，提示对大幅度空间偏移仍然脆弱。
- **手动特征设计**：教师特征选取SigLIP2+DINOv2，未探讨其他视觉基础模型的效果差异。

（完）
