---
title: "DWM: Separating World Effects from Actions in Latent World Models"
title_zh: DWM：在潜在世界模型中分离世界效应与动作
authors: "Yi-Ge Zhang, Tianqi Du, Qi Zhang, Yisen Wang"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18715v1"
tags: ["query:world-model"]
score: 9.0
evidence: 在潜在世界模型中分离世界效果与动作，用于基于模型的控制
tldr: "当前潜在世界模型将动作效应与世界效应混为一谈，导致学习效率低下。DWM提出一种监督级分解方法，通过添加动作不变的世界头和正交约束，实现预测过渡的显式加法分解。在三个包含持久世界效应的W变体基准中，DWM的CEM规划成功率平均提升13.1%，且无需改动基础架构。该方法首次在潜在空间明确分离动作因果，提升了模型的可解释性和迁移能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1403, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1429, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 603, \"height\": 491, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1219, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1103, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 798, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1254, \"height\": 435, \"label\": \"Table\"}]"
motivation: 现有潜在世界模型将动作驱动与固有动态混合，限制了模型对因果的归因能力和迁移性。
method: DWM在预测器中增加世界头，以对比学习正则化使之动作不变，并与原预测头正交，实现过渡的加法分解。
result: "在三个W变体基准上，DWM在CEM规划成功率上平均领先基线13.1%。"
conclusion: DWM无需改变架构即可显式分离动作效应与世界效应，提升模型可解释性和泛化性。
---

## 摘要
潜在世界模型是现代基于模型的控制的基础，然而当前基于动作的条件公式使用单一的、无差异的目标来监督下一个潜在状态的转移，迫使一个整体学习信号吸收状态变化的每个来源。然而，在现实世界中，转移来自两个异质来源：由智能体引起的动作驱动组件，以及动作不变的世界效应——即在零动作下仍然会发生的变化，由环境的固有动态决定（例如，重力驱动的滑动、惯性、接触反弹和持续漂移）。将它们合并到一个单一目标中会在潜在转移中纠缠两者，阻止模型将观察到的变化归因于其根本原因，并削弱所学动态的可迁移性。我们提出了DWM（分解世界模型），这是一个操作化这种分解的监督级别框架。DWM用一个辅助的世界头增强了潜在世界模型的预测器，通过归一化的世界对比目标进行正则化以保持动作不变性，而原始的预测头通过正交性约束与它耦合；两者一起将预测转移显式地分解为动作不变组件和补充的动作驱动组件，而不改变底层架构或推理流程。为了在持续的世界效应下评估DWM，我们构建了三个标准控制基准的W变体——PushT-W、Reacher-W和TwoRoom-W——每个都实例化了一个独特的动作不变动态。DWM在平坦对应任务上与强基线匹配，并在W变体上实现了CEM规划成功率的平均绝对提升13.1%。

## Abstract
Latent world models underpin much of modern model-based control, yet current action-conditioned formulations supervise   the next-latent transition with a single, undifferentiated target, forcing a monolithic learning signal to absorb every   source of state change. In real world, however, transitions arise from two heterogeneous sources: an action-driven   component induced by the agent, and an action-invariant world effect -- the change that would still occur under a null   action, dictated by the environment's intrinsic dynamics (e.g., gravity-driven sliding, inertia, contact rebound, and   persistent drift). Fusing them into a single target entangles the two inside the latent transition, prevents the model   from attributing observed changes to their underlying causes, and undermines the transferability of the learned   dynamics. We introduce DWM (Decomposed World Model), a supervision-level framework that operationalizes this   decomposition. DWM augments the predictor of a latent world model with an auxiliary world head, regularized by a   normalized world-contrastive objective to be action-invariant, while the original pred head is coupled to it via an   orthogonality constraint; together, the two signals induce an explicit additive decomposition of the predicted   transition into an action-invariant and a complementary action-driven component, without altering the underlying   architecture or inference pipeline. To evaluate DWM under persistent world effects, we construct W-variants of three   standard control benchmarks -- PushT-W, Reacher-W, and TwoRoom-W -- each instantiating a distinct action-invariant   dynamic. DWM matches strong baselines on the flat counterparts and delivers a mean absolute improvement of 13.1% in CEM   planning success across the W-variants.

---

## 论文详细总结（自动生成）

# 论文总结：DWM：在潜在世界模型中分离世界效应与动作

## 1. 核心问题与整体含义（研究动机和背景）

- **现有限制**：当前主流的基于动作条件的潜在世界模型（如 LeWM）使用一个单一的、无差异的目标来监督下一个潜在状态的预测。这使得模型被迫将**动作驱动的变化**（由智能体引起）和**动作不变的世界效应**（如重力、惯性、漂移等环境固有动态）融合在同一个学习信号中。这种纠缠导致模型无法将观察到的状态变化正确归因于其根本原因（是智能体操作还是环境自主动态），从而降低了所学动态的可迁移性和多步推演的准确性，尤其在持续存在世界效应的环境中，规划性能会显著下降。
- **研究动机**：作者提出，在潜在空间层面显式分离这两种异质来源的转移，可以提升潜在世界模型在物理控制场景中的预测质量和规划能力。
- **整体含义**：本文首次在监督级（而非架构级）解决世界效应与动作效应的分离问题，旨在提高模型的可解释性、泛化性和在现实动态环境中的实用性。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在训练阶段引入一个辅助的**世界头（world head）**，它被训练为对当前动作不变（即只捕获世界效应），同时通过**正交性约束**将其与原始预测头（pred head）的输出解耦，从而实现预测转移的显式加法分解：`predicted_next_latent = world_component + action_component`。推理时世界头被丢弃，架构不变。
- **关键技术细节**：
  - **架构**：保持原编码器 φ、动作条件预测器 g 和预测头 h_p 不变。在训练时并行添加一个轻量级 MLP 世界头 h_w，与 h_p 共享相同的预测器状态 r_t。
  - **世界对比损失 L_wc**：通过动作扰动构造正负样本对。对同一历史上下文，用当前动作 a_t 和随机扰动后的动作 ã_t 分别通过预测器和世界头得到两个视图 ˆz_w 和 ˜ˆz_w，使用对称 InfoNCE 损失鼓励世界头输出对动作不变，同时通过负样本区分不同状态，防止坍塌（温度 τ=0.07）。
  - **正交损失 L_orth**：定义为 `|cos(ˆz_w, ˆz_a)|`，其中 ˆz_a = ˆz - ˆz_w（动作驱动组件），强制两者正交，确保世界头捕获的动作不变信息与残差互补，而不是被吸收到同一方向。
  - **整体目标**：`L = L_WM + λ_wc L_wc + λ_orth L_orth`，其中 L_WM 是原世界模型的下一潜在预测损失与正则项。默认 λ_wc=0.3，λ_orth=0.5。
  - **算法流程**：训练时，每个批次的每个上下文，从批次内随机置换当前动作得到扰动动作，计算世界头输出并应用上述损失。推理时仅使用预测头，与基线完全相同。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - **W变体任务**：为三个标准控制基准添加持续的世界效应：
    - **PushT-W**：在推桌子任务中加入重力驱动滑动（重力向量 (45,25)，阻尼 0.92），块在空操作下仍会滑动。
    - **Reacher-W**：在 DMC Reacher 中加入垂直平面重力（z 方向 g= -9.81*sin60°），使机械臂持续受下拉力。
    - **TwoRoom-W**：在 2D 导航中加入恒定漂移偏置 `b=(4,2) pixels/step`，智能体不操作时仍会漂移。
  - **平坦对照任务**：移除世界效应的原版（PushT、DMC Reacher、TwoRoom），用于检验方法在无世界效应时是否退化。
  - **Ball-in-Cup**：DMC 标准任务，摆锤式振荡，具有状态依赖的非线性世界效应，作为更复杂的扩展测试。
- **Benchmark**：自定义基准套件，包括上述三对 flat/W 变体任务外加 Ball-in-Cup。
- **对比方法**：以 **LeWM**（基线单头潜在世界模型）为主要对比，另在消融中比较自身变体、OOD 重力测试等。实验设置确保相同骨干（ViT-tiny + 6层 Transformer 预测器）、相同训练协议和相同 CEM 规划配置。

## 4. 资源与算力

- **GPU 型号**：NVIDIA H20。
- **精度与批量**：bfloat16 混合精度训练，batch size 128。
- **训练时长**：未给出绝对时间，但按任务分别设定了固定 epoch 数：PushT/PushT-W 5 epoch，Reacher/Reacher-W 20 epoch，TwoRoom/TwoRoom-W 100 epoch，Ball-in-Cup 20 epoch。论文未说明 GPU 数量或总训练小时数。
- **总结**：论文提供了训练超参数和硬件信息，但缺少总计算量的量化细节（如 GPU 天数）。可以认为资源中等规模。

## 5. 实验数量与充分性

- **实验数量**：约 4 组主要规划实验（3 W变体 + 平坦对照 + Ball-in-Cup），每组包含 3 个 CEM 种子和 50 个 start-goal pairs，共几百次评估。此外还包括：
  - 表示分离诊断（表 1）：3个 W变体，对比方差/跨度比。
  - 多步预测质量（表 2）：离线一步和 rollout@20 损失。
  - OOD 重力泛化（图 4）：训练重力 (45,25)，测试 (90,0) 和 (0,45)。
  - 消融实验（表 3）：改变 λ_wc 和 λ_orth 共 6 个设置（含 0 值）。
  - Ball-in-Cup 额外对比（表 4）。
- **充分性评估**：实验设计比较全面，覆盖了主效果、消融、OOD 泛化和表示分析。使用了独立验证集、多个随机种子和统计检验（如 z-test）。公平性方面：对比基线使用相同训练预算、相同 CEM 协议；Ball-in-Cup 为消除成功头偏差，为每个模型单独训练成功头。不足的是未在其他下游任务（如强化学习）或不同骨干上验证，也未与现实世界数据对比。

## 6. 主要结论与发现

- **规划性能提升**：在三个 W 变体任务上，DWM 的 CEM 规划成功率平均绝对提升 **13.1 个百分点**（PushT-W +12.0pp，Reacher-W +10.7pp，TwoRoom-W +16.7pp）。在平坦对照任务上性能持平，证明方法在不必要时不产生副作用。
- **表示分离成功**：世界头对动作的方差比预测头低两到三个数量级（见表 1），说明成功学得了动作不变的世界效应。
- **预测质量改善**：DWM 的多步 rollout 误差在所有 W 变体上显著降低（如 PushT-W 的 rollout@20 误差降低 66.7%）。
- **OOD 泛化**：在推桌子 W 变体上，即使重力方向改变，DWM 仍保持稳定的动作驱动分量和较高的 CEM 成功率（44% vs 基线 30%），说明分解具有鲁棒性。
- **消融验证**：两个损失项（L_wc 和 L_orth）均不可或缺，单独减少任一个都会导致性能下降。

## 7. 优点

- **方法轻量且正交**：仅修改训练目标，不改变推理架构，可轻松适配现有潜在世界模型。
- **问题定义清晰**：首次明确将“世界效应”与“动作效应”作为异构来源分离，并提供了正式定义（基于空动作的反事实）。
- **实验设计严谨**：
  - 构建对照任务（flat vs W-variant）以隔离变量。
  - OOD 测试验证分解的泛化性。
  - 消除评估偏误（Ball-in-Cup 独立成功头）。
- **表示分析深入**：不仅报告规划成功率，还通过方差/跨度比、余弦相似度等诊断解耦质量。
- **结果一致且显著**：在三种不同类型的持续世界效应（重力滑动、垂直重力、恒定漂移）上均取得一致提升，且统计显著。

## 8. 不足与局限

- **实验覆盖范围有限**：
  - 仅在基于 CEM 的规划任务上验证，未测试在强化学习、模仿学习或实际机器人系统中的适用性。
  - 所有 W 变体都是人为构造的（如恒定重力或漂移），真实世界效应可能更复杂（如非线性、状态依赖、时变）。
  - 未与现有解耦方法（如 Iso-Dream）进行直接对比，仅在单一基线 LeWM 上比较。
- **方法假设**：
  - 加法分解假设（`ˆz = ˆz_w + ˆz_a`）在强非线性耦合下可能不成立，残留相互作用被简单吸收到动作驱动分量中。
  - 世界头的动作不变性依赖于动作扰动采样策略（随机置换批次内动作），在动作空间特别大或连续时可能效果受限于采样密度。
  - 需要调整两个超参数（λ_wc, λ_orth），虽然在消融中展示了敏感性，但未提供自动调参策略。
- **潜在偏差**：
  - 训练数据全部来自随机策略，未测试规划时的分布偏移（如规划策略与训练策略行为差异）。
  - Ball-in-Cup 上的提升较小（+6 pp），且可能因成功头单独训练而引入额外方差，削弱结论强度。
- **可复现性**：论文提供了架构和超参数细节，但未公开源码或预训练模型，也未说明数据生成的具体代码链接。

（完）
