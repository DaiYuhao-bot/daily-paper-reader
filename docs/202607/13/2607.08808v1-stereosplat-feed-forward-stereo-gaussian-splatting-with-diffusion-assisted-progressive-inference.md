---
title: "StereoSplat+: Feed-Forward Stereo Gaussian Splatting with Diffusion-Assisted Progressive Inference"
title_zh: "StereoSplat+: 前馈立体高斯泼溅与扩散辅助渐进推断"
authors: "Zihua Liu, Masatoshi Okutomi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08808v1"
tags: ["query:world-model"]
score: 8.0
evidence: 前馈立体高斯泼溅用于3D场景重建
tldr: 现有3D高斯泼溅（3DGS）方法通常需要多视角观测或未来帧的非因果访问，阻碍了在机器人、AR等仅单立体对场景中的应用。本文提出StereoSplat+，包含一个输入可变的前馈3D高斯估计器StereoSplat，以及一个扩散增强的单次渐进推理方案，通过渲染新视图并扩散增强后迭代更新。在KITTI-360数据集上，该方法在遮挡区域和强视角外推下显著提升了新视角渲染质量和几何精度，实现了仅从单立体对进行因果的高质量3DGS场景重建，性能优于现有前馈基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1829, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 251, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1722, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 875, \"height\": 523, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1741, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08808v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 377, \"label\": \"Table\"}]"
motivation: 现有3DGS依赖多视图或未来帧，无法在仅单立体对下高质量重建，限制了实际部署。
method: 提出StereoSplat前馈估计器融合代价体积和三平面分支，结合扩散增强渐进推理，从单立体对逐步生成新视图并优化。
result: 在KITTI-360上，新视图PSNR提升约1.5dB，遮挡区域和强外推场景几何精度显著改善。
conclusion: 仅从单立体对即可因果重建高质量3DGS场景，超越现有前馈基线，适用于实时AR/机器人平台。
---

## 摘要
近期三维高斯泼溅（3DGS）的进展使得用于新视角合成的高质量、可渲染场景表示成为可能。然而，大多数现有3DGS管线依赖多视角观测（或对未来帧的非因果访问）以实现充分覆盖，这在设备端机器人和增强现实设置中通常不可用，因为感知仅限于单个立体相机。因此，从单一立体观测中恢复高质量3DGS场景仍然具有挑战性，原因包括遮挡、有限的视野和缺失的几何结构。我们提出StereoSplat+，一种扩散增强的前馈框架，能够从单个立体对实现因果重建。我们的方法基于两个关键组件。首先，我们提出StereoSplat，一种输入不变的前馈三维高斯估计器，它接受可变数量的有姿态立体对作为输入，并预测高质量的三维高斯。StereoSplat通过代价体积分支和基于三平面的三维体积分支融合互补的几何线索，并利用连续姿态编码以泛化到不同视角数量和相机配置。其次，由于推断时通常无法获得多个有姿态立体对，我们引入一种扩散增强的一次性渐进推断方案，称为StereoSplat+：从单个立体对开始，我们从预测的3DGS渲染新的立体视图，使用一步扩散增强器对其进行细化，并将它们作为额外输入反馈以更新3DGS。在KITTI-360数据集上的实验表明，StereoSplat+提高了新视角渲染质量和几何精度，特别是在遮挡区域和强外推视角下，优于近期前馈3DGS基线方法。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled high-quality, render-ready scene representations for novel-view synthesis. However, most existing 3DGS pipelines rely on multi-view observations (or non-causal access to future frames) to achieve sufficient coverage, which is often unavailable in on-device robotics and AR settings where sensing is restricted to a single stereo rig. Recovering a high-quality 3DGS scene from one stereo observation, therefore, remains challenging due to occlusions, limited field of view, and missing geometry. We present StereoSplat+, a diffusion-enhanced feed-forward framework that enables causal reconstruction from a single stereo pair. Our method builds on two key components. First, we propose StereoSplat, an input-invariant feed-forward 3D Gaussian estimator that takes a variable number of posed stereo pairs as input and predicts high-quality 3D Gaussians. StereoSplat fuses complementary geometry cues via a cost-volume branch and a triplane-based 3D volume branch and leverages continuous pose encoding to generalize across view counts and camera configurations. Second, since multiple posed stereo pairs are typically unavailable at inference time, we introduce a diffusion-enhanced one-shot progressive inference scheme called StereoSplat+: starting from one stereo pair, we render novel stereo views from the predicted 3DGS, refine them with a one-step diffusion enhancer, and feed them back as additional inputs to update the 3DGS. Experiments on the KITTI-360 dataset show that StereoSplat+ improves novel-view rendering quality and geometry accuracy, especially in occluded regions and under strong view extrapolation, outperforming recent feed-forward 3DGS baselines.

---

## 论文详细总结（自动生成）

# 论文结构化总结：StereoSplat+

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的前馈三维高斯泼溅（3DGS）方法通常需要多视角观测（或对未来帧的非因果访问）来实现充分的场景覆盖，但在实际机器人、AR/VR等设备端场景中，传感输入往往仅局限于单个立体相机（单一立体对），导致严重的遮挡、视野受限和几何缺失问题，无法高质量重建场景。
- **整体含义**：本文旨在实现从单立体对进行**因果、实时**的3DGS场景重建，使前馈3DGS管线能在资源受限的部署条件下仍能生成高质量的新视角合成和几何估计，为实时在线重建和增量式大规模建图提供可行方案。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将**输入不变的前馈3DGS估计器**与**扩散增强的渐进推断**相结合。先由单立体对估计初始3DGS，再渲染伪新视图并经过单步扩散模型增强，然后作为额外输入重新估计，最终通过置信度融合得到更优的3DGS。
- **StereoSplat（输入不变3DGS估计器）**：
  - 采用**双分支架构**：代价体积分支（保留精确立体匹配线索） + 三平面Transformer 3D体积分支（在3D空间聚合证据，处理遮挡和弱纹理区域）。
  - 两个分支通过投影引导融合（cost-volume高斯特征注入三平面）实现互补。
  - 支持**可变数量输入立体对**（1~K对），使用**连续正弦姿态编码**（对平移和旋转进行位置编码）代替固定大小嵌入，并结合**随机视图子采样训练**（随机选择输入对的数量和位置）实现输入不变性。
  - 联合估计深度和3DGS属性（中心、不透明度、颜色、旋转、缩放、置信度），无需外部深度估计器。
- **StereoSplat+（扩散增强渐进推断）**：
  - 一次“渲染–增强–再注入”流程：从单立体对估计初始3DGS → 渲染伪新立体视图 → 使用预训练的**单步扩散增强器**（基于SD-Turbo微调）改善伪视图质量 → 将增强后的视图作为额外输入重新运行StereoSplat → 对两次估计的3DGS进行**置信度引导融合**得到最终结果。
  - 训练时采用**伪视图混合训练**：随机用当前模型渲染的伪立体对替换真实立体对，减少训练-推断分布差异。
- **公式/算法流程**（文字描述）：
  1. 输入：单个立体对（I_left, I_right, 姿态T）。
  2. 特征提取：共享CNN + 预训练DepthAnythingV2提取图像和几何特征。
  3. 代价体积分支：构建多视图相关代价体积，预测深度和初始3DGS属性（基于像素对齐）。
  4. 三平面分支：基于可学习三平面特征，通过交叉视图注意力（CIDA）和跨平面注意力（CPDA）聚合3D证据，预测体素级3DGS属性。
  5. 融合：将代价体积分支的高斯投影到三平面，融合后得到G_base。
  6. 渐进推断：渲染伪新视图 → 单步扩散增强 → 将增强视图追加到输入（此时输入变为多对） → 重新估计得G_plus → 置信度融合得最终3DGS。
- 训练损失：渲染RGB L2损失 + LPIPS + 深度L1损失 + 置信度损失，深度监督使用LiDAR稀疏深度和伪密集深度。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：**KITTI-360**（城市驾驶基准，提供矫正立体图像和稀疏LiDAR）。排除动态严重的序列（0003, 0007, 0010），使用剩余6个序列。将每段轨迹按8米长度划分为bin，每个bin选取第一帧立体对作为输入，其余帧（中心、最后一帧）作为新视角目标。共54,862个bin（训练49,377，验证5,485）。
- **基准（评估指标）**：
  - 新视角RGB：PSNR、SSIM。
  - 几何精度：深度AbsRel、SqRel（使用LiDAR投影深度作为真值）。
- **对比方法**：当前最先进的前馈3DGS基线：
  - PixelSplat [4]、MVSplat [5]、OmniScene [2]、DepthSplat [3]。
  - 均在相同设置下仅使用第一帧立体对作为输入，比较bin内所有帧的平均指标。

## 4. 资源与算力

- 文中明确说明：使用**4块NVIDIA A6000 GPU**，PyTorch框架，训练30个epoch，batch size为4，使用AdamW优化器，初始学习率8×10⁻⁵，共训练约**1,450,000次迭代**。启用了梯度检查点减少内存。
- 消融实验部分使用KITTI-360序列0000（9,207训练/1,023评估bin）单独训练。

## 5. 实验数量与充分性

- **消融实验**（Table I）：对代价体积分支/3D体积分支/融合/随机视图子采样/扩散渐进推断分别进行隔离实验，共6组（Branch CV、Branch 3D、CV+3D、StereoSplat、StereoSplat+），在序列0000上评估。
- **全量对比实验**（Table II）：与4种基线方法在完整KITTI-360验证集上比较，报告bin平均PSNR/SSIM/AbsRel/SqRel。
- **定性可视化**：图4、5、6展示了遮挡区域、强外推（鸟瞰图）下的可视化对比，定性支持定量结果。
- **额外演示**：图7展示了增量大规模3DGS重建潜力（bin-wise融合）。
- **充分性与公平性**：
  - 消融实验覆盖了所有主要组件，且控制变量。
  - 对比基线使用官方或复现设置，输入条件一致（仅第一帧立体对）。
  - 评测指标全面（RGB+深度、多个帧位置）。
  - 但仅在一个数据集（KITTI-360）上评测，缺少对室内场景（如Replica、ScanNet）或其他驾驶数据集（如nuScenes）的测试，范围有限。

## 6. 论文的主要结论与发现

- StereoSplat+在仅使用单个立体对作为输入的情况下，明显优于所有现有前馈3DGS方法，bin平均PSNR提升约1.7 dB（20.76 vs 19.04），SSIM提升0.09（0.69 vs 0.60）。
- 几何精度同样提升（AbsRel从0.094降至0.071（StereoSplat）或0.096（StereoSplat+），SqRel大幅降低）。
- 扩散增强渐进推断进一步改善了遮挡区域和强外推视角下的渲染质量与几何一致性。
- 随机视图子采样训练策略提升了模型对输入视图数量和位置的不变性。
- 双分支融合比单一分支在远离输入视角（中心帧/最后一帧）时表现更稳定。

## 7. 优点

- **方法创新性**：首次将输入不变的前馈3DGS估计器与扩散增强渐进推断结合，实现单立体对因果重建，对实际部署有重要意义。
- **双分支架构设计精巧**：代价体积保证局部精度，三平面体积处理全局遮挡和3D推理，且通过投影引导融合紧密耦合。
- **训练策略实用**：随机视图子采样、连续姿态编码、伪视图混合训练共同保证了输入不变性和分布鲁棒性。
- **扩散使用轻量高效**：仅用单步扩散增强伪视图，不进行迭代优化，保持前馈效率。
- **实验充分**：消融实验清晰验证每个组件贡献，定量定性结果一致，对比基线全面且指标覆盖外观和几何。

## 8. 不足与局限

- **仅验证了驾驶场景（KITTI-360）**：缺乏室内、多物体、动态场景的泛化实验，结论可能限于该领域。
- **扩散增强可能引入伪影**：单步扩散模型偶尔会生成不真实细节，且多次渐进更新会累积误差（作者也承认这一点）。
- **未显式建模动态物体**：如行人、车辆运动导致时间不一致性，影响增量式建图。
- **深度监督依赖伪密集深度**：使用预训练NMRFStereo生成伪标签，存在误差传播风险；且LiDAR稀疏性限制了深度监督质量。
- **大规模建图仅初步展示**：图7的增量融合并未进行定量评估，实际场景下的漂移和一致性问题未分析。
- **单数据集、单相机配置**：未测试不同基线距离、不同分辨率、不同立体相机模型的影响。

（完）
